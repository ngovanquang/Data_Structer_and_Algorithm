#include <bits/stdc++.h>

using namespace std;

int main(int argc, char** argv){
    int T, L, C, temp;
    cin >> T;
    vector<int, vector<int, int>> data;
    for (int i = 0; i < T; i++)
    {
        cin >> L >> C;
        for (int j = 0; j < 2*L; j++)
        {
            for (int k = 0; k < C; k++)
            {
                cin >> temp;
                data
            }
            
        }
        
    }
    

}
